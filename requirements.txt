# Core dependencies for Exelsias - Python Backend
# =============================================
# OPTIMIZED: Removed unused packages to reduce build size
# Note: GUI is now handled by Electron

# Database & Models
pydantic>=2.5.0,<3.0.0

# AI/LLM (REQUIRED - do not remove)
# For universal CPU (no AVX2): see BUILD_UNIVERSAL.md
# For Windows CPU: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cpu
# For CUDA: pip install llama-cpp-python --extra-index-url https://abetlen.github.io/llama-cpp-python/whl/cu121
llama_cpp_python>=0.2.0

# numpy is required by llama-cpp-python
numpy>=1.24.0,<2.0.0

# EPUB processing (for import feature)
ebooklib>=0.18,<1.0.0

# Utilities (minimal set)
requests>=2.31.0,<3.0.0
regex>=2023.0.0
filelock>=3.13.0,<4.0.0
packaging>=23.0,<25.0
typing_extensions>=4.9.0,<5.0.0

# Audio/TTS (REQUIRED - do not remove)
edge-tts>=6.1.0,<7.0.0
pygame>=2.5.0,<3.0.0
piper-tts>=1.2.0

# Build/Deploy (install separately when building)
# pip install pyinstaller

# =============================================
# REMOVED (not used in codebase):
# - transformers, tokenizers, safetensors, huggingface-hub (not imported)
# - spacy, nltk (not imported)
# - pandas (not imported)
# - pillow (not needed - frontend handles images)
# - fpdf2 (not imported)
# - soundfile (not needed - edge-tts handles audio)
# - tqdm (not imported)
# - PyYAML (not imported)
# =============================================
